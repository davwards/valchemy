{
  "name": "valchemy",
  "version": "0.1.0",
  "description": "An extensible, presentation-agnostic validation system",
  "main": "index.js",
  "scripts": {
    "test": "jasmine && eslint --ignore-pattern spec/ **/*.js && eslint --env jasmine spec/**/*.js"
  },
  "keywords": [
    "validation"
  ],
  "author": "David Edwards",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^2.2.0",
    "jasmine": "^2.3.2"
  },
  "dependencies": {
    "lodash": "^4.3.0"
  }
}
